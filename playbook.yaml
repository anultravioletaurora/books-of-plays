# Always perform same generic linux role
- name: 🌌 Cosmonautical Ansible Playbook
  hosts: all
  tasks:
    - ansible.builtin.include_role: debug
      when: debug | default(false)
      
    - ansible.builtin.include_role: "{{ ansible_facts[distribution| lower] }}"
